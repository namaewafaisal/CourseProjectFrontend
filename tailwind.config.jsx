// tailwind.config.js
import { defineConfig } from 'tailwindcss'

export default defineConfig({
  theme: {
    extend: {
      colors: {
        bg: '#121212e8',
        text: '#ffffff',
        login: 'rgba(38, 210, 38, 0.909)',
        register: 'rgb(32, 108, 202)',
      },
    },
  },
  plugins: [],
})
